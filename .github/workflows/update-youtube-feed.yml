name: YouTube Feed Update

on:
  schedule:
    # Run once an hour at minute 7
    - cron: '7 3-19 * * *'
  workflow_dispatch: # Manual trigger

jobs:
  update-youtube:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Fetch YouTube
      env:
        YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_CREDENTIALS }}
      run: node .github/scripts/fetch_youtube.js

    - name: Commit Change
      run: |
        git config user.name "F1 Bot"
        git config user.email "bot@boxboxbox.app"
        git add f1_youtube.json
        timestamp=$(date -u)
        git commit -m "YouTube Feed update: ${timestamp}" || exit 0
        git push
